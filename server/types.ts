export interface TokenResponse {
    userId: string;
    email: string;
    firstName: string;
    lastName: string;
    profileImageUrl: string;
    accessToken: string;
    refreshToken: string;
  }